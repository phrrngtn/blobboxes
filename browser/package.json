{
  "name": "blobboxes-browser",
  "version": "0.1.0",
  "private": true,
  "description": "Browser bundle for text bbox extraction and domain classification",
  "main": "src/index.js",
  "scripts": {
    "build": "node build.js",
    "build:lite": "node build.js --no-roaring"
  },
  "dependencies": {
    "roaring-wasm": "^1.1.0"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  }
}
